version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - libpoco-devel
    - libeigen3-devel

sphinx:
   configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .